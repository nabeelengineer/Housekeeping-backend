# Server
PORT=4000
CORS_ORIGIN=http://localhost:5173

# Database
DB_HOST=127.0.0.1
DB_NAME=housekeeping
DB_USER=root
DB_PASS=secret
DB_DIALECT=mysql

# Auth / JWT (if applicable)
JWT_SECRET=change_me

# Development only: enable sync (migrations preferred)
USE_SYNC=false
